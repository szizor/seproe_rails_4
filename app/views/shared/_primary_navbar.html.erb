<nav class="navbar navbar-obscure margin-bottom">
  <div class="container-fluid">

      <div class="navbar-header">
        <a class="navbar-brand" href="/">Compartiendo Espacios</a>
      </div>

          <% if !user_signed_in? %>
            <%= render "shared/primary_guest"%>
          <% elsif current_user.adoptant? %>
            <%= render "shared/primary_adoptant"%>
          <% end %>
      
  </div>

</nav>